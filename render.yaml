services:
  - type: web
    name: ai-chat
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node api/server.js
    envVars:
      - key: NODE_VERSION
        value: 18
